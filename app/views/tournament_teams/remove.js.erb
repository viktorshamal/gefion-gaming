$('#btn-trn-signup').popover('show');
$('#trn-teams').html("<%= escape_javascript(render partial: 'tournament_teams/teams', locals: {:tournament => @tournament}) %>");
$('.trn-sign-up-popover').html("<%= escape_javascript(render partial: 'tournament_teams/signup', locals: {:tournament => @tournament}) %>");
$('#btn-trn-signup').popover('hide');